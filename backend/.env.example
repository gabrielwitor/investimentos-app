# Backend Environment Variables
NODE_ENV=development
PORT=3001

# Database Configuration
# For Docker development, use 'db' as hostname
# For local development, use 'localhost' (requires MySQL container running)
# To start just the database: docker-compose up -d db
DATABASE_URL="mysql://app_user:app_password_secure@db:3306/investimentos"
